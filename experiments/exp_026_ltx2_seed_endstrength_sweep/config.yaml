# exp_026 — LTX-2 Seed × End-Clip Strength Sweep — PLACEHOLDER (not implemented)
#
# Forked from exp_024 design. To implement:
#   - Replicate exp_024/run.py structure
#   - Outer loop: samples; inner loop: cartesian (seeds × end_clip_strengths)
#   - Lock positive prompt to "" and use exp_025-winning negative prompt

model:
  model_id: "Lightricks/LTX-2"
  distilled_lora_strength: 1.0

# Same diagnostic subset as exp_025. Fill in once we return to implementation.
samples: []

inputs:
  num_clip_frames: 25
  start_clip_strength: 1.0
  positive_prompt: ""
  negative_prompt: ""   # placeholder — replace with exp_025 winner

  end_clip_strengths: [1.0, 0.85, 0.7]

inference:
  height: 512
  width: 768
  num_frames: 193
  frame_rate: 24.0
  num_inference_steps: 40
  guidance_scale: 3.2
  run_stage2: true

runtime:
  seeds: [42, 123, 7, 2025]

outputs:
  dir: "outputs/videos/exp_026_ltx2_seed_endstrength_sweep"
