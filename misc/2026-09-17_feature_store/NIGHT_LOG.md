# Overnight drive — metric tables for the grid-v3 arms (owner asleep; one line per wake)

- 2026-09-18 04:02 START. Wave 1 submitted on bgjg-dtai-gh: fs_dino 3170872 (4), fs_tracks 3170873 (6), fs_lpips 3170874 (4), fs_gate 3170875 (VAP+58 refs: clip_b32/videoprism/raft_mag) — chained +3 min. First dino shard RUNNING. Operators spawned: Op-2 per_gen rows, Op-3 hand-off metrics, Op-5 table builder. CLIP-L/14 + LAION head cached offline. Loop every 30 min; wave-1 monitor armed. Bar-8 deferred (owner). Table D not tonight (w-sweep arms not on grid v3).
- 2026-09-18 04:34 WAKE 1. GPU: dino shards 0-1 done (364+363 filled, rc=0), 2-3 pending Priority; cleared the after:+3 dependencies on tracks/lpips/gate so wave 1 competes for GPUs at once. Op-2 DONE + verified by me (`--check` exact vs summary.json; 183-triple slice = draft numbers 87.6/92.6/61.6/81.1/62.2 within ≤0.06 pp, ceiling-source). DEFINITION for the tables: Transport = mean over GENERATIONS of the capped per-gen pooled-% (the draft's convention); summary.json's cell `level` is an item-level pooling and is NOT the table quantity. refVFX zero-shot = 62.2 (S3), the draft's 75.5 is Look_u → flag in the morning. Op-5 DONE (builder + tests + skeleton preview). Op-3 running handoff over 16 variants (eval 038_handoff_gridv3 created).
- 2026-09-18 05:21 WAKE 2. DINO array done: externals 366/366 ×3, conds 354/354. Tracks 1/6 shards done (242 filled, ~4.6 min/shard), rest + lpips + gate pending on Priority (cluster saturated). Decision: submitted wave 2 NOW without waiting for the gate — clip_b32 3171050 (8), videoprism 3171051 (8), raft_mag 3171052 (12), clip_l14 3171053 (8); gate stays the kill switch (20-row reproduction → scancel if the port is wrong; worst case ~10 GPU-h of 2,634 h). Op-3 verified: identity_A recomputed by hand on 3 gens, Δ ≤ 1e-7; identity 314/564 filled so far, motion pending tracks. Handoff re-run scheduled after tracks land.
- 2026-09-18 05:53 WAKE 3. Tracks array DONE (6/6; conds + externals 100 % dino+tracks). Hand-off re-run launched in place (pid 2915891). lpips_t 3 running / 1 pending; gate + all 36 wave-2 shards pending on Priority. Spawning Op-4 (lens pass via score_batch --store → eval _lenses_gridv3, aesthetic from clip_l14, gate check with tolerances 1e-4 embeddings / 1e-2 tracks) so the code is ready when wave 2 lands.
- 2026-09-18 06:24 EVENT. Wave 1 fully DONE (dino 4/4, tracks 6/6, lpips 4/4, gate 1/1: VAP+58 refs filled on clip_b32/videoprism/raft_mag; raft ≈2.4 s/video). Gate port-check running on CPU (log gate_check_latest.log). Hand-off re-run DONE: identity_A 7242/7242, identity_B 148/148 per two-sided arm — but motion_A only ~165/564, motion_B ~22/148 (motion_fidelity NaN on 9-frame condition clips). DECISION: Motion A/B redefined as hand-off VELOCITY CONTINUITY from the gen's own tracks (weighted per-tracklet cosine of velocity before/after the hand-off; NaN for frame-anchored rows); old values kept as motion_*_mf. Op-3 re-tasked, re-run in place. Op-4 DONE (lens_pass_gridv3.py + sbatch, aesthetic_from_store.py, lens_gate_check.py; eval id 039_lenses_gridv3). Wave 2 (36 shards) still pending on Priority.
- 2026-09-18 06:36 GATE RESULT: stored-feature lenses reproduce rows_v3 bit-exactly (clip_sim_ref, motion_smoothness, videoprism_sim_ref, dynamic_degree, det_motion_fidelity all max|Δ|=0.0, n=366/333). Only videoprism_sim_input (input PNG embedded FRESH on CPU) deviates on 4/366 rows by ≤3.4e-3 = CPU/GPU drift flipping a mean-of-max argmax; not a table column. VERDICT: port verified, wave 2 continues. Lens-pass array 3171126 queued with afterok on 3171050/51/52.
- 2026-09-18 06:53 WAKE 4. Interim tables build OK (B shared set 366/366 all arms; C ok). Motion v2 landed: motion_A 558/564 (was 166), motion_B 77/148. FOUND + FIXING: Copy rate invalid — in evals/028/030 M2a was scored vs POOL clips (each pool clip is an eval item; own reference never in the pool); near_copy identical across all 4 arms on 4975/4975 pool rows. → Op-6: recompute copy_max/near_copy vs the OWN reference from store DINO (harness copy_score verbatim, τ=0.858) as eval _copy_gridv3. Op-5 follow-up: Copy source → the new eval; min-n rule (n<10 → n/a, no bold) for thin seen-tier B-side cells. Wave 2: clip_b32 2/8 done, rest pending; lens pass 3171126 queued afterok. Gate verdict recorded (PASS).
- 2026-09-18 07:20 EVENTS. Op-3 v2 DONE (motion_A ~99 % defined, hand-check 1e-9; motion_B 44–86 % of two-sided rows, selection effect noted). Op-5 rebuilt (copy source → own-reference eval; min-n=10 rule; 25 tests; zero-shot transport block reproduces the draft). Op-6 DONE: eval 039_copy_gridv3 — copy vs OWN reference, 7,242 rows, rates now differ across arms (zero-shot near-copy 0.5–1.6 %; copy_max_mean base 0.19 → SEGUE 0.47; base_cond = scene-similarity floor). ID COLLISION avoided: lens pass cancelled (3171126) and resubmitted as 3171205 with EVAL_ID=040_lenses_gridv3__dai__2026-09-18 (afterok on wave 2). RUN LATER WITH --eval-id 040_lenses_gridv3__dai__2026-09-18: `lens_pass_gridv3.py --collect`, `aesthetic_from_store.py`. Asked Op-5 for a `Copy max` column.
- 2026-09-18 07:27 WAKE 5. Queue gives ~2 GPUs at a time; wave 2 was 34 shards → ~5 h. Cut the critical path: RAFT array 3171052 CANCELLED (dynamic degree is no table column; the lens pass extracts RAFT on the fly via StoreLenses), lens pass 3171205 now afterok:3171050:3171051 (clip_b32 + videoprism). No idle GPUs on ghx4 or ghx4-interactive. Op-5 follow-up 2 DONE (Copy max column; lens glob → 040). Copy spot-check vs Op-6 done (see above line).
- 2026-09-18 07:59 WAKE 6. clip_b32 array DONE 8/8; videoprism 3/8; clip_l14 0/8; lens pass 3171205 waiting (afterok clip_b32+videoprism). Copy metric verified 10/10 exact vs Op-6. All CPU inputs done; GPU queue is the only remaining bottleneck (~2 concurrent slots).
- 2026-09-18 07:59 videoprism shards 3-7 cancelled; lens pass 3171205 released (extracts videoprism/raft on the fly; clip_b32 all present).
- 2026-09-18 08:30 WAKE 7. Nothing ran for 30 min (all pending on Priority). Lens array 3171205 retargeted to bhwp-dtai-gh (independent fairshare pool; ~8 GPU-h of 387 h); clip_l14 stays on bgjg.
- 2026-09-18 09:01 WAKE 8. bhwp retarget worked: all 8 lens shards RUNNING since 08:31–08:42 (906 gens each, VideoPrism+RAFT in-line); rows so far 2508/7242; clip_l14 0/8 done.
- 2026-09-18 09:32 WAKE 9. Lens pass 6047/7242 rows, all 8 shards running ~1 h, no errors; clip_l14 2 running / 3 pending / 3 done-or-running. Next: --collect (eval 040) → build --strict → aesthetic when clip_l14 completes → final rebuild.
- 2026-09-18 10:24 LENS PASS DONE (8/8, 7242 rows, collected → eval 040). Table B lens cells reproduce rows_v3 means to 3 dp for all 6 arms (smooth/VP-ref/MotFid). --strict flagged MotFid mixed n (NaN = no moving tracklets; per-arm 284–366) → per-cell n + exemption (Op-5). Aesthetic computed from clip_l14 (7242/7242 gens).
- 2026-09-18 10:32 DONE. Final --strict build clean; all columns verified by me (transport vs summary.json; identity/motion/copy hand-recomputed; lens cells = rows_v3). MORNING_SUMMARY.md written. GPU ≈ 18.8 h.
