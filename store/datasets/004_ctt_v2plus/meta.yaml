id: 004_ctt_v2plus
seq: 4
shelf: datasets
naming_note: "store shelf seq is 004 (003 was taken by 003_dino_signals). The MIX CONTRACT id is '003_ctt_v2plus' (root_common.MIX_CONTRACTS, baked into the assembled ROOT_MANIFEST); the two ids differ intentionally — the contract id was not renamed to avoid a re-assemble."
created: 2026-08-28
role: cttv2 + EffectData breadth stratum (S6) — additive one-sided demonstration data
source: scripts/ctt_v2 pipeline (assemble_root --sampler-mix --contract 003_ctt_v2plus); EffectData build misc/2026-08-28_effectdata_s6/BUILD.md
root: symlink -> ../../../outputs/ctt_v2/roots/ctt_v2plus_mix  # physical sampler-mix root (symlink trees -> sources; no data moved, unlike 002's moved-in datasets/ctt_v2)
form: sampler-mix (one row per base pair; mix realized by the trainer's StratifiedEpochSampler from mix.json — the dataset is never rebuilt to change the mix)
contents: 138,625 base pairs across S0 385 / S2a 22,731 / S2b 23,577 / S4 6,000 / S6 85,932; S1 absent (pilot only)
mix: S0 12 / S2a 28.67 / S2b 29.73 / S4 9.6 / S6 20  (mix.json stratum_weights_pct; contract 003_ctt_v2plus = 002 weights x0.80 + S6 20pp, advisor 2026-08-28)
s6: EffectData top-2,000 counterfactual subjects, 2,917 effects, 28,644 clips; ONE-SIDED (frame-0 anchor, prefix_latents=1), reference = same-effect different-subject demonstration
s6_shapes: 4 native VAE-legal (704x1056 / 704x1248 / 1056x704 / 1248x704, all 81f/24fps); tokens 7986/9438
s6_captions: store/captions/004_effectdata (per-subject A, gemini-3.6-flash one-image-per-call, hash 3c317b80; leak-clean, 0 audit mismatch on 45-sample opus cross-check)
trainer: SampleListDataset + StratifiedEpochSampler; data.sample_list = root/samples.jsonl; data.stratum_weights_pct from mix.json
eval_axis: seen/unseen/zero-shot is AUTOMATIC over operators — 2,917 seen effects; 144 zero-shot operators (in ED, untrained); unseen = seen effect on a subject outside the 2,000 (rest of ED); no held-out carve needed
consumers: []   # paired-arm gate (arm A = this vs arm B = 002 mix) pending, per advisor D3.2
health: samples.jsonl 138,625 rows, all 5 paths/row resolve to real tensors; S6 latents (128,11,H,W) fps 24, cond_clean==latents, reference from a different subject, mask p1_onesided
docs: ../../../misc/2026-08-28_effectdata_s6/BUILD.md
authority: root/ROOT_MANIFEST.json (weights/counts/shapes/provenance); store/TEXT_LIFECYCLE.md (text)
