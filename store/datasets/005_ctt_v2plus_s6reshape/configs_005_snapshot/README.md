# configs_005 snapshot (tracked provenance copy — 2026-08-30)

Byte copies of `misc/2026-08-27_dino_signal_training/configs_005/{A0..A5}.yaml` + `PINS.env` (the DeltaAI-side arm configs that
consume this dataset; `misc/` is gitignored, so this is their only tracked record) and of `build/train_chunk.sbatch` (the self-stop
2×GH200 chunk script) at the moment the 005 chains were launched (independent Fable check PASS, 2026-08-30 ~18:10 UTC).
The live files are the authority; this folder is provenance. eps renders its own variants (`eps/configs_005`, platform paths + accum 2).
Pins: SAMPLES_SHA256 048d1ef4… (138,147 rows) · NORM_SHA256 db47be88… (NORM_dino_v4) · MIX S0=2,S1=18,S2a=22.09,S2b=22.91,S4=8,S6=27.
