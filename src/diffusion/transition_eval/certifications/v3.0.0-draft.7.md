# Certification record — transition-eval/3.0.0-draft.7

**Overall: FAIL** · bars sha256 `5ab7a427e5f3b0fb…` · corpus sha256 `aa28c6d5a6a744a7…` · commit `17a7b09a1d58`

| bar | verdict |
|---|---|
| bar1_m1a_floor | FAIL |
| bar2_siblings | PASS |
| bar3_controls | FAIL |
| bar4_splices | FAIL |
| bar5_reversal | PASS |
| bar6_m3_panel | FAIL |
| bar7_copy_twins | FAIL |
| bar8_integration_determinism | FAIL |

**What certification claims, exactly:** *the instrument separates the styles it has seen (per-class trust map attached), refuses known-degenerate and copied inputs, is direction-sensitive on motion, runs deterministically end-to-end on real generations, and its blind spots are enumerated.* It does not claim: that metrics track human judgment (M4 exempt until O9); that pools/masks behave identically on generated-domain frames (untestable without labels); M2c validity (first real training manifest); M1b absolute validity (injected-trajectory test = post-lock appendix).

- mask winner: **v3_sided** · motion winner: **v3_decomposed** · O7 Huber triggered: True
- content-invariance pooled partial corr: 0.8176084265617223 (alarm 0.4, non-gating)
- tau_copy recalibrated: None (splice scoring crashed)
- sigma_seed: PENDING (gates first model report, not this tag)
- Block C: 0 archived items scored (scoring crashed); 97 convertible, 53 excluded loudly (see record.json)

Record assembled post-hoc by re-executing the frozen certify/ graders over the run's artifacts after the driver crash (see `post_hoc_assembly` in record.json).

## Failure diagnosis (fail-forward: bars untouched; fixes -> draft.8)

1. **bar1 (M1a floor 0.80/1.5): FAIL** — acc 0.673, d 1.52 on the adopted v3_sided mask (envelope: 0.578/1.32). The frozen floor was anchored to the v2 precedent 0.851/d=2.22 (exp_054), which was measured on a 47-clip / 11-style corpus at chance 0.213; this exam is 223 clips / 39 classes at chance 0.067 with fine-grained confusable families. Old-corpus classes still retrieve well; the floor was mis-calibrated at freeze time, not the metric degraded.
2. **bar3 (controls): FAIL 26/37** — the floor clause holds 36/37 (controls score below their sibling; the one inversion is nature_bloom, the content-confound outlier), but the `core_degenerate` clause fires on 26/26 static-hold controls and only 1/11 lerp controls: pixel-blend lerp frames are far from BOTH endpoints in DINO space and earn 8-29 sub-threshold core frames. The clause encoded a false hypothesis about lerps.
3. **bars 4/6/7 + crashes (bar8): FAIL** — one scorer bug: object_match's tracklet keep-filter can come back empty on low-texture video (splice loops, some archived generations) and _smooth_tracks then crashes instead of returning NaN. Killed cert_probes and cert_blockc scoring wholesale.
4. **bar8 anchor rule bug** — 'first two-sided / first one-sided / first camera n>=4 class' picks air_bending twice (first in two strata); the 6-anchor assertion correctly refused. Warm rerun itself: max |delta| None vs tolerance 1e-6 -> PASS.
5. **bar5 (reversal): PASS** — genuine verdict, computed from the run's tracked reversed videos: sign test (n=15, p=0.0176, alpha=0.05), wins 12, losses 3, median drop 0.15631843994327466.
6. **O7 triggered** — camera-stratum mean recall 0.346 < 0.5: the Huber IRLS secondary analysis is mandatory in the next draft.
7. **Content-invariance alarm (non-gating)** — pooled partial corr 0.818 vs alarm 0.4: within-class M1a variation substantially tracks content overlap. Must be carried as a caveat wherever M1a is reported.

Artifacts: exam/, content_invariance.json, manifests/, probe_videos/, cert_*/items.jsonl, record.json — all under `outputs/eval/certification/3.0.0-draft.7/` (job 9463686 log alongside).