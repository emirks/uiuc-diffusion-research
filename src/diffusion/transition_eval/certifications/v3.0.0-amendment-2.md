# v3.0.0 — Amendment 2: owner-validated sidedness relabels (data plane)

**Date:** 2026-07-16. **Scope:** two per-class sidedness labels in
`corpus_manifest.json`. **Instrument code, mask-S semantics, all certified
metrics, bars, τ_copy, and the eval/v3.0.0 tag: untouched.**

## What changed and why

The taxonomy v2 owner validation (2026-07-16, all 39 classes signed off in the
viewer; protocol `docs/taxonomy/PROTOCOL_v2_PROPOSAL.md`, ADOPTED) resolved the
9 tracked sidedness conflicts. Net relabels against the corpus manifest:

| class | manifest (was) | owner-final | basis |
|---|---|---|---|
| giant_grab | onesided | **twosided** | hand re-enters in the B window |
| hero_flight | onesided | **twosided** | flight persists into the B window |

The raw labeled tree keeps its original directory names; the relabel is applied
as a documented `OWNER_SIDEDNESS_OVERRIDES` map in `build_corpus_manifest.py`
(main) and the manifest was rebuilt deterministically — diff is exactly these
two class-level fields, zero clip-level changes.

- corpus_manifest sha256 (was): `e7c867a6a4269679…`
- corpus_manifest sha256 (now): `348db23dac72d7ed…`

## Operational rule for scoring

Metric code runs from the certified checkout (tag `eval/v3.0.0`) as always;
`--corpus` MUST point at the corrected manifest (`348db23dac72d7ed…`). The
versioning stamp hashes the manifest actually passed, so every result row
self-documents which corpus it used. Scoring any ladder rung with the stale
manifest is an error (hero_flight items would get the a_only core mask instead
of the envelope mask).

## Impact assessment

1. **S mask:** giant_grab and hero_flight items switch core-mask mode
   a_only → envelope at scoring time. This is the *intended* effect of the
   relabel — no certified constant depends on it.
2. **σ_seed (amendment-1) caveat:** hero_flight was σ_seed roster row 8, drawn
   into the "onesided-camera" stratum (items hero_flight_6/hero_flight_8) and
   scored under the a_only mask. The roster rule stays frozen (drawn blind; no
   re-draw). Remedy: when the first scoring batch runs, rescore the hero_flight
   σ_seed item under the corrected label in the same GPU session and report
   pooled σ_seed both ways; the pooled estimate (≈48 df) is expected to be
   robust to one item. σ_seed is calibration context, not a bar — fail-forward
   applies, no retroactive gate.
3. **Per-sidedness control floors:** any floor pool that included giant_grab or
   hero_flight control items under 'onesided' shifts composition. Flagged to
   the v4 certification effort (in flight) for recomputation there; for
   v3.0.0-scored results the floors carry this note.
4. **Ladder (Amendment 1 of the plan):** consistent — hero_flight's grid record
   already carried `sidedness_key: two_sided` pending validation; validation
   confirmed it, so B1 (hero_flight R2/R3 + R5) generates with the pre-built
   keying, no retraining, no item re-draw. B8 (one_sided ∧ swap=F) re-verified
   against the owner-final taxonomy: all 8 recipients unchanged.

## Provenance

Owner validation trail: `outputs/taxonomy/corrections.json` exports (21:07 and
22:03 snapshots) folded into `outputs/taxonomy/class_axes_v2.yaml`
(`scripts/build_class_axes_v2.py` reproduces it); fold reports alongside.
